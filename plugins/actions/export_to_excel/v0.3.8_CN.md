# v0.3.8 版本发布说明

本次发布主要为 Open WebUI 0.9.x 提供兼容支持。

## 兼容性
- **兼容 Open WebUI 0.9.x**：运行时自动检测 Open WebUI 版本，并根据不同版本适配数据库调用。
- **异步数据库修复**：`Users.get_user_by_id` 和 `Chats.get_chat_by_id` 现在会根据 Open WebUI 版本选择正确的 async / sync 调用路径。
