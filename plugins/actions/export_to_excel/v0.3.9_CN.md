# v0.3.9 版本发布说明

本次发布新增了导出 Excel 文件的可配置行高和列宽选项。

## 新功能
- **可配置行高**：新增 `ROW_HEIGHT` 配置项，可控制 Excel 导出中数据行的高度。设为 `0`（默认）根据内容自动调整，或设置固定值（如 `20`）以获得紧凑的单行显示。
- **可配置列宽**：新增 `COLUMN_WIDTH` 配置项，可控制 Excel 导出中的列宽。设为 `0`（默认）根据内容自动调整，或设置固定值（如 `15`）以获得统一的紧凑列宽。
